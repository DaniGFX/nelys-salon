# Disable default auto-detected providers to prevent Nixpacks from launching Apache
providers = []

[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} router.php"
